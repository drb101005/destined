{
  "name": "adaptive-low-bandwidth-communication-platform",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "concurrently -k \"npm run dev --workspace server\" \"npm run dev --workspace client\"",
    "build": "npm run build --workspace client",
    "call": "npm run build --workspace client && npm run start --workspace server",
    "deploy:pages": "npm run build --workspace client && gh-pages -d client/dist",
    "start": "npm run start --workspace server",
    "test": "npm run test --workspace client"
  },
  "devDependencies": {
    "concurrently": "^9.1.0",
    "gh-pages": "^6.3.0",
    "vitest": "^4.1.10"
  }
}
